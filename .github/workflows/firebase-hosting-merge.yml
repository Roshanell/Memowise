# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
env:
  VITE_APP_API_SERVER_URL: ${{ env.VITE_APP_API_SERVER_URL }}
  VITE_APP_AUTH0_DOMAIN: ${{ env.VITE_APP_AUTH0_DOMAIN }}
  VITE_APP_AUTH0_CLIENT_ID: ${{ env.VITE_APP_AUTH0_CLIENT_ID }}
  VITE_APP_CLIENTSECRET: ${{ env.VITE_APP_CLIENTSECRET }}
  VITE_APP_AUTH0_CALLBACK_URL: ${{ env.VITE_APP_AUTH0_CALLBACK_URL }}
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm i && npm run build
        working-directory: ./client
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MEMOWISE }}'
          channelId: live
          projectId: memowise
          entryPoint: ./client
